# Optional local Qwen runtime.
# Install only for GPU/on-prem images after requirements-torch.txt.
huggingface_hub==1.3.0
tokenizers==0.22.2
safetensors==0.7.0
transformers==5.0.0rc3
accelerate==1.13.0
qwen-vl-utils==0.0.14
